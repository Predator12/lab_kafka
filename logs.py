import logging


class Log:

    @staticmethod
    def send_data(summons_number, plate_id, registration_state, plate_type, issue_date,
                  violation_code, vehicle_body_type, vehicle_make, issuing_agency, row_id):

        pass
        # logging.basicConfig(format='%(asctime)s - %(levelname)s - %(message)s', level=logging.INFO)
        # logging.info(f'LOG DATA FOR {row_id} ROW')
        # logging.info(f'summons_number={summons_number}')
        # logging.info(f'plate_id={plate_id}')
        # logging.info(f'registration_state={registration_state}')
        # logging.info(f'plate_type={plate_type}')
        # logging.info(f'issue_date={issue_date}')
        # logging.info(f'violation_code={violation_code}')
        # logging.info(f'vehicle_body_type={vehicle_body_type}')
        # logging.info(f'vehicle_make={vehicle_make}')
        # logging.info(f'issuing_agency={issuing_agency}')
        # logging.info('.............................................................')
